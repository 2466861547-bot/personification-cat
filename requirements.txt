# personification-cat 依赖
# Python >= 3.12 (推荐 3.13/3.14)
#
# 注意: torch / torchaudio 版本随 Python 版本变化:
#   Python 3.12 → torch 2.5.1, torchaudio 2.5.1
#   Python 3.13 → torch 2.6+, torchaudio 2.6+
#   Python 3.14 → torch 2.9+, torchaudio 2.9+
#
# macOS 专用替换:
#   vllm    → mlx-lm + vllm-mlx (Apple Silicon 原生优化, 2-3x 性能)
#   autoawq → llama.cpp GGUF Q4_K_M (Metal 原生支持)
#   fairseq → faster-whisper + transformers (已在依赖中)

# 音频处理
librosa>=0.10.2
soundfile>=0.12.1
torchaudio>=2.5.1
audiomentations>=0.35.0

# ASR 语音识别
openai-whisper>=20240930
faster-whisper>=1.0.3

# 大模型框架
torch>=2.5.1
transformers>=4.46.3
peft>=0.13.2
datasets>=3.1.0
accelerate>=1.1.1
trl>=0.12.1

# 音频生成
bark>=0.1.5
# CosyVoice 声纹克隆: 自包含实现，不依赖 PyPI cosyvoice 包
# 需要: torch, transformers, modelscope, safetensors, librosa, pyyaml, sentencepiece
modelscope>=1.18.0
safetensors>=0.4.0
sentencepiece>=0.1.99
# fairseq>=0.12.2     # 已由 faster-whisper + transformers 替代

# RAG 检索增强
llama-index>=0.12.5
chromadb>=0.5.20
sentence-transformers>=3.3.1
jieba>=0.42.1

# LangChain Agent 编排 (兼容 0.3.x 和 1.x)
langchain>=0.3.7
langchain-community>=0.3.7
langchain-core>=0.3.15

# 推理优化 (macOS Apple Silicon 专用)
# vLLM 的 macOS 替代: MLX 框架 (Apple 官方, 2-3x 性能)
mlx>=0.26.0
mlx-lm>=0.26.0
# vllm-mlx>=0.1.0    # 高并发场景可选 (PagedAttention for Apple Silicon)
# llama.cpp 通过 Metal 后端提供 GGUF Q4_K_M 量化 (替代 autoawq)

# 定时调度
schedule>=1.2.2

# 工具库
pyyaml>=6.0.2
numpy>=2.1.3
pandas>=2.2.3
pydantic>=2.10.3
tqdm>=4.67.1
tensorboard>=2.18.0
scipy>=1.14.1

# Web 接口
fastapi>=0.115.5
uvicorn>=0.32.1
