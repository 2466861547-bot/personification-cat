# 拟人化文本生成 - 架构决策说明

## 1. 原始设计 vs 当前实现

| 方面 | 原设计（LLM） | 当前实现（模板） |
|------|--------------|-----------------|
| **数据源** | LoRA 微调的 LLM | 手写模板库 |
| **灵活性** | 高（理论上可生成任意文本） | 有限（只能从预设模板中选择） |
| **可靠性** | 低（会生成乱码、错误内容） | 高（100% 可预测） |
| **风格一致性** | 差（不同调用可能生成不同语言） | 好（统一的可爱口语风格） |
| **品种感知** | 差（经常忽略 breed 参数） | 好（每个品种有专属昵称） |

## 2. 写死了什么？

### 2.1 品种特定的口语习惯（breed_hints）

```python
breed_hints = {
    # 品种名: [昵称1, 昵称2]  ← 用于生成拟人化文本
    "橘猫": ["橘胖", "小胖橘"],
    "英短": ["英短", "蓝胖子"],
    "布偶": ["布偶", "小仙女"],
    "金毛": ["金毛", "大暖男"],
    "柯基": ["柯基", "小短腿"],
    # ... 共 18 个品种
}
```

**作用**：当用户传入 `--breed 橘猫` 时，模板会自动使用"橘胖"或"小胖橘"作为拟人化主语，例如：
```
橘胖：铲屎官，我的饭碗空啦~快喂我！
```

### 2.2 情绪模板（每个品种 × 情绪 = 约 5 条模板）

```python
templates = {
    "hungry": [
        "{昵称}：铲屎官，我的饭碗空啦~快喂我！",
        "{昵称}用爪子扒拉你的腿：我饿了啦，别装没看见！",
        "喵~ 主人，你闻到我肚子咕咕叫了吗？",
        # ... 5 条
    ],
    "happy": [
        "喵呜~ 今天阳光好好，主人摸摸我的头嘛~",
        "{昵称}翻出肚皮：看！我给你展示我的肚皮！",
        # ... 5 条
    ],
    # ... 20 种情绪
}
```

**覆盖**：
- 猫咪：20 种情绪 × 平均 4 条模板 ≈ 80 条
- 狗狗：20 种情绪 × 平均 4 条模板 ≈ 80 条
- **总计**：约 160 条手写模板

### 2.3 兜底逻辑

```python
# 未知情绪 → 智能映射
fallback = {
    "very_happy" → "happy",
    "sad_feeling" → "sad",
    "angry_mood" → "angry",
}
```

## 3. 为什么不用 LLM？

### 3.1 你提供的实际案例

```
输入: --pet cat --breed (未指定)
LLM 输出: "宠物类型: dog 品种: 棒棒喂食兽兽棒棒喂食兽兽..."
```

问题：
1. **宠物类型错误**：输入 cat，输出 dog
2. **重复无意义文本**：`棒棒喂食兽兽` 重复 N 次
3. **多语言乱码**：`这 Биография当前的状态 Bischof 压断了喔雪上台`
   - `Биография` = 俄语"传记"
   - `Bischof` = 德语"主教"

### 3.2 根本原因

| 问题 | 原因 |
|------|------|
| **训练数据不足** | 你的 LoRA 微调可能只用了几百/几千条样本，LLM 没有学会稳定的拟人化风格 |
| **Prompt 工程缺陷** | LLM 接收的 prompt 没有正确约束输出格式，导致它"自由发挥" |
| **多语言干扰** | 基础模型（LLaMA 等）是多语言的，在中文能力不足时会输出其他语言的 token |
| **幻觉问题** | LLM 倾向于重复训练数据中高频出现的模式，如 `棒棒喂食` 可能来自某条训练数据 |

## 4. 模板系统的优势

### 4.1 可靠性保证

```
输入: pet=cat, breed=橘猫, emotion=hungry
输出: 橘胖：铲屎官，我的饭碗空啦~快喂我！
```

- ✅ 宠物类型正确（猫咪）
- ✅ 品种昵称正确（橘胖）
- ✅ 情绪正确（hungry → 讨饭）
- ✅ 中文自然语言（无乱码）

### 4.2 可维护性

如果想添加新的情绪或品种，只需：
```python
# 1. 添加品种
"萨摩耶": ["萨摩耶", "微笑天使"],

# 2. 添加情绪模板
"excited": [
    "{昵称}转着圈儿蹦跶：好开心呀~",
],
```

### 4.3 可测试性

模板系统可以 100% 覆盖测试：
```python
for pet in ["cat", "dog"]:
    for breed in all_breeds:
        for emotion in all_emotions:
            result = generate(pet, breed, emotion)
            assert breed in result  # 确保品种名出现
```

## 5. 改进方向

### 5.1 混合方案（推荐）

```
情绪分类 → 模板生成基础文本 → LLM 润色 → 最终输出
```

这样可以：
- 保留模板的可靠性（宠物类型、品种、情绪正确）
- 利用 LLM 的创造力（生成更自然、更多样的表达）

### 5.2 动态模板

从 JSON/YAML 文件加载模板，而非硬编码：
```json
{
  "breeds": {
    "橘猫": ["橘胖", "小胖橘"]
  },
  "emotions": {
    "hungry": {
      "cat": ["{hints}：铲屎官，我的饭碗空啦~"]
    }
  }
}
```

### 5.3 真正的 LLM 方案

如果坚持使用 LLM，需要：
1. 收集 10,000+ 条高质量拟人化文本
2. 正确的 Prompt 工程（约束输出格式）
3. 增加 `repetition_penalty` 防止重复
4. 使用 `temperature=0.3` 降低随机性
5. 后处理过滤多语言 token

## 6. 结论

| 方案 | 适用场景 |
|------|---------|
| **模板系统（当前）** | MVP 阶段、需要稳定输出、快速开发 |
| **LLM 方案** | 已有大量训练数据、需要创造性表达 |
| **混合方案** | 兼顾可靠性和灵活性（推荐） |

当前选择模板系统是因为它是最快、最可靠的解决方案。如果后续需要更丰富的表达，可以考虑混合方案。